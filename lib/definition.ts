export type User = {
    id: bigint;
    username: string;
    email: string;
    password: string;
}

export type Movie = {
    movietitle: string;
    moviedate: string;
    movievote: string;
    movieimage: string;
};

